# OPS-NET-003：新客户端迁移与管理入口保护

5080 安装新客户端并向 4090 商业源网借网，客户接口 snadca3ca00719e，地址 10.213.40.3；d408 保留已安装客户接口 sna33074e2e9416，地址 10.213.40.2。4090 安装客户端入口但继续作为源机，不向自己借网。

4090 的旧 wg-d408 和旧 helper 方案已停用、移入 `/var/backups/sna-legacy-cleanup-20260916T193417`。5080 将旧 wg-fleet 改为管理辅助专用，移除全局 /1；wg0 管理组网保留。不能删除仍供 Titan 使用的源机 wg-fleet/gateway。

d408 旧 wg-d408、na 配置、helper 方案、旧启动服务与控制脚本已移至 `/var/backups/sna-legacy-cleanup-20260916T194132`。归档时控制脚本和配置目录同名导致一次移动失败，随后将配置目录单独归档为 d408-network-config 并重新加载 systemd；新客户端连接保留。

## ERR-NET-004：管理隧道入口被新客户出口接管

迁移后管理入口 140.143.202.144 的 UDP 被新客户隧道接管，wg0 超过八分钟没有握手，c201-5080-wg 及其跳板连接超时。此前仅检查管理网段路由，没有验证外层入口路径，是验证遗漏。

修复：5080 的 wg-fleet AllowedIPs 持久设置为 `10.203.49.0/24,140.143.202.144/32`，为云端入口加专用主机路由，继续通过此前可用路径承载管理入口。其他公网走新客户端。修复前配置备份 `/var/backups/sna-management-endpoint-20260916T194045`。

实测：wg0 重新握手；到 10.201.250.1 三次 ping 全部成功约 18 ms；Reliable SSH 经 wg 路由成功读取 5080 身份，随后 d408 和 Titan 跳板身份读取均成功。新客户公网路由与 HTTPS 在切换时验证通过。开机持久性未重启验证。

这次保留了管理辅助隧道及 Titan 所需旧源端网关，不能称为删除所有旧 WireGuard 配置。客户订阅有效期 30 天；5080 当前不限流量，签发默认速度上限下载 50,000,000 B/s、上传 10,000,000 B/s。

后续共性要求：迁移必须同时验证管理隧道内层网段和外层 Endpoint 的路由，保持入口路径独立于待切换公网出口。完整通用客户端实现尚需跟进，当前是部署兼容保护。

## OPS-NET-004：5080 客户端重启、退网与再次借网复测

只重启 server-network-assist-client-1000.service，没有重启操作系统。重启后客户端保留活跃租约及客户隧道。随后调用真实客户端退网 API：客户隧道删除，active=null，公网路由恢复 via 10.20.32.1/enp4s0；租约释放成功，无 release_error。退网期间校园直连 d408 SSH 成功，管理 WG 身份读取和 d408/Titan 跳板访问均成功。

ERR-NET-005：首次退网 API 超过 40 秒不返回，但实际退网已完成。定位到收尾 connectivity(True) 使用 urllib，而其连接超时不能限制系统 DNS 解析耗时。修复 client.py 的在线退网收尾探测为后台 daemon 线程、最多等待 5 秒；超时返回 ok=null/timed_out=true，表示公网状态未知，不能误报退网失败或公网不可用。补丁部署于 5080，原文件备份 `/var/backups/client-before-disconnect-probe.py`；其他机器未部署。

修复后实际“连接→退网”接口约 5.3 秒成功返回，active=null，公网恢复物理网关，再次连接成功。最终恢复 5080 向 4090 借网。此为客户端进程重启与功能循环验收，不能替代操作系统开机验证，也未执行校园账号登录或保证退网后校园公网已认证。

ERR-NET-006：再次借网后的 HTTPS 首次超时/TLS 失败，客户 WG 已握手、源机 HTTPS 正常。降低客户 MTU 1420→1280 后百度 HTTPS 返回 200，结果支持路径报文大小兼容问题，尚未抓包证明具体 PMTU 丢包位置。给客户 client-routing.json 增加 mtu=1280，client_tunnel.install 支持本地 MTU 覆盖，已部署 5080。随后再次重启客户端并完成退网→借网循环；退网接口约 5.3 秒成功，租约释放无错误。操作系统 boot_id 前后均 6770c8a5-326a-441e-98e0-7d31a263c01e，确认未重启服务器。

原 client_tunnel.py 备份 `/var/backups/client-tunnel-before-mtu.py`。源码补丁未发布或提交 GitHub；其他客户端的 MTU 未修改。
